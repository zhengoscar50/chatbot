# Pinned to the versions the test suite passes against.
#
# Unpinned, a fresh install on a server pulls whatever is newest that day —
# a different FastAPI or Pydantic than anything was tested with. Re-pin
# deliberately (bump, run `pytest`, commit) rather than letting it drift.
argon2-cffi==25.1.0
fastapi==0.128.8
httpx==0.28.1
pydantic-settings==2.11.0
PyJWT==2.13.0
python-dotenv==1.2.1
python-multipart==0.0.20
uvicorn[standard]==0.39.0
